export interface Harvest {
    id: number
    cropId: number
    cropLabel: string
    farmId: number
    quantity: number
    quantityUnit: string
    seasonId: number
    seasonLabel:string
    userId: number
    userType: string
}